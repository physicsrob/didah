/**
 * Learn Mode UI Components (Phase 0 Stub)
 *
 * This is a placeholder implementation for Phase 0.
 * Actual implementation will be done in Phase 5.
 */

import type { SessionSnapshot } from '../../runtime/io';
import type { UIContext } from '../shared/types';

export function LearnDisplay(_props: { snapshot: SessionSnapshot }): React.ReactNode {
  // Stub implementation - will be implemented in Phase 5
  return (
    <div>
      <p>Learn Mode Display (not yet implemented)</p>
    </div>
  );
}

// eslint-disable-next-line react-refresh/only-export-components
export function useLearnInput(_context: UIContext): void {
  // Stub implementation - will be implemented in Phase 5
  // No-op for now
}
