// Import shared types from backend (single source of truth)
export type { UserSettings, FeedbackMode } from '../../../../functions/shared/types'
export { DEFAULT_USER_SETTINGS } from '../../../../functions/shared/types'